# 🎬 MovieLens + OMDb ETL Pipeline (AI-Assisted)

This project demonstrates a complete, production-style **ETL (Extract-Transform-Load) pipeline** that enriches MovieLens metadata by integrating data from the **OMDb API**, caches results, stores output in SQLite, and exports enriched datasets for downstream analysis.

---

## 🚀 Project Features

### ✔ Extract
- Loads MovieLens dataset (movies + ratings)
- Optionally pulls metadata from OMDb API
- Uses offline caching to avoid API limits

### ✔ Transform
- Cleans MovieLens titles
- Maps OMDb metadata to MovieLens
- Computes enriched movie information (ratings, metadata, genres, etc.)

### ✔ Load
- Writes all tables to a **SQLite database (`movies.db`)**
- Creates normalized schema using `schema.sql`
- Saves CSV exports (e.g., `movies_enriched_export.csv`)

---

## 📂 Repository Structure
## 📂 Repository Structure


movie_etl_project/
│
├── etl.py # Main ETL pipeline script
├── movies.db # SQLite database generated by ETL
├── requirements.txt # Python dependencies
├── README.md # Documentation (this file)
├── schema.sql # SQL schema for DB creation
├── queries.sql # Sample SQL queries
│
├── data/ # (Optional) raw data folder
│ └── README.md
│
├── sql/ # SQL scripts folder
│ └── README.md
│
├── output/ # ETL outputs and exports
│ └── README.md
│
└── sample_output/ # Example outputs provided
└── README.md


---

## 🛠 How to Run the ETL

### 1️⃣ Create virtual environment

### 2️⃣ Install dependencies

### 3️⃣ Add your OMDb API key  
Create a `.env` file:

### 4️⃣ Run ETL with real API  

### 5️⃣ OR run ETL in mock/offline mode  

---

## 🗄 Database Schema (Simplified)

- `movies`
- `ratings`
- `movies_enriched`

Full schema in: **schema.sql**

---

## 🧪 Sample Queries

Example: Show top 10 movies by rating:

More queries in: **queries.sql**

---

## 📝 Notes
- OMDb API has request limits; use `--mock-omdb` mode to avoid errors.
- SQLite DB (`movies.db`) is portable and works with DB Browser for SQLite.
- This project simulates a real production ETL workflow.

---

## 🧑‍💻 Author
Anand Reddy  
AI-assisted ETL Data Engineering Project


